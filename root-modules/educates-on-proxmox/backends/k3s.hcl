# backend configuration pointing to the specific flavor build directory
path = "build/k3s/k3s.tfstate"

# Example if we want to use an external S3
#bucket = "my-bucket"
#key    = "states/k3s.tfstate"
#region = "eu-central-1"